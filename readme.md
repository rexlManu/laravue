# Laravue (Laravel + VueJS)
The laravel framework with the vue.js framework implemented ready for use.
##Setup
- Clone the repository
- Create the env
- `php artisan migrate`
- `npm install`
- `npm run dev`
- `php artisan serv`
- Enjoy ;)
